import React from 'react'

function FilterBar() {
    return (
        <div>
            <select>
                <option>Anime title options</option>
            </select>
            <h2>Search by Name: <input></input></h2>
            <select>
                <option>Filter options</option>
            </select>
        </div>
    )
}

export default FilterBar